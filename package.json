{
    "private": true,
    "devDependencies": {
        "@web/test-runner": "^0.19.0",
        "serve": "^14.2.4"
    },
    "scripts": {
        "test": "npm run test:local",
        "test:local": "web-test-runner \"test/**/*.test.js\"",
        "test:ci": "web-test-runner \"test/**/*.test.js\"",
        "open": "serve public/"
    }
}
